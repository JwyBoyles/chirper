import {React} from "react";

export default function NoPage() {
    
    return(
        <div>
            <p>You have eneded up on page that has no function please click on the home button to return to the feed</p>
        </div>
    )
}